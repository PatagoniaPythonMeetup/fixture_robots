query Robot($key: String) {
  fixture {
    robot(key: $key) {
      key
      nombre
      escuela
      encargado {
        nombre
        dni
        email
        rol
      }
      escudo
    }
  }
}